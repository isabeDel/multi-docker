sudo: required 
language: generic
services: 
    - docker

before_install:
    - docker build -t isabeDel/multi-docker -f Dockerfile.dev .

script:
    - docker run -e CI=true isabeDel/multi-docker npm test